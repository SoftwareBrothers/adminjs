import { ResourceOptions, FeatureType } from 'admin-bro'
import { ContentParent } from '../../parents'

export const features: Array<FeatureType> = []

export const options: ResourceOptions = {
  navigation: ContentParent,
}
