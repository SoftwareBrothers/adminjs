import React, { ReactNode } from 'react';
declare type Props = {
    children: ReactNode;
    location?: string;
};
declare const TestContextProvider: React.FC<Props>;
export default TestContextProvider;
